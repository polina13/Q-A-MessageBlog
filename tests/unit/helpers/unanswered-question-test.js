import { unansweredQuestion } from 'qa/helpers/unanswered-question';
import { module, test } from 'qunit';

module('Unit | Helper | unanswered question');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = unansweredQuestion([42]);
  assert.ok(result);
});
